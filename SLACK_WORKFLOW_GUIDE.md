# Slack 워크플로우 설정 가이드

Slack 워크플로우가 시작될 때 Notion에 자동으로 작업을 추가하는 방법을 안내합니다.

## 📋 Notion 데이터베이스 설정

먼저 Notion에서 다음 필드들을 가진 데이터베이스를 생성하세요:

### 필수 필드들:
- **Name** (제목) - Title 타입
- **작성자** - Rich Text 타입  
- **채널** - Rich Text 타입
- **워크플로우** - Rich Text 타입
- **트리거타입** - Select 타입 (옵션: manual, workflow, event, scheduled)
- **상태** - Select 타입 (옵션: 할 일, 진행중, 완료)
- **생성일** - Date 타입

## 🔧 Slack 워크플로우 설정

### 1. 워크플로우 생성
1. Slack에서 "워크플로우 빌더"로 이동
2. "워크플로우 만들기" 클릭
3. 워크플로우 이름 입력 (예: "작업 생성")

### 2. 트리거 설정
다음 중 하나를 선택하여 워크플로우를 시작할 수 있습니다:

#### A. 키워드 트리거
- 트리거: "키워드"
- 키워드: `!작업` 또는 `!task`
- 채널: 원하는 채널 선택

#### B. 단축키 트리거  
- 트리거: "단축키"
- 단축키 이름: "작업 추가"
- 설명: "Notion에 새 작업 추가"

#### C. 예약 트리거
- 트리거: "예약"
- 빈도: 매일, 매주 등 설정

### 3. 웹훅 액션 추가
1. "단계 추가" → "웹훅" 선택
2. 웹훅 설정:
   - **URL**: `https://your-domain.com/api/slack-webhook`
   - **HTTP 메서드**: POST
   - **헤더**: `Content-Type: application/json`

### 4. 페이로드 설정
웹훅 페이로드에 다음 JSON 구조를 사용하세요:

```json
{
  "payload": {
    "task_description": "{{작업 내용}}",
    "user_name": "{{트리거한 사용자}}",
    "channel_name": "{{채널 이름}}",
    "workflow_name": "{{워크플로우 이름}}",
    "trigger_type": "workflow"
  }
}
```

### 5. 변수 설정
워크플로우에서 사용할 변수들을 설정하세요:

- **작업 내용**: 사용자 입력 필드 (텍스트)
- **트리거한 사용자**: 자동 변수
- **채널 이름**: 자동 변수  
- **워크플로우 이름**: 고정값 또는 자동 변수

## 🚀 배포 및 테스트

### 1. 서버 배포
```bash
# Vercel에 배포
npm install -g vercel
vercel

# 또는 다른 플랫폼 사용
```

### 2. 환경변수 설정
배포된 서버에 다음 환경변수를 설정하세요:
- `NOTION_API_KEY`: Notion Integration Token
- `NOTION_DATABASE_ID`: Notion 데이터베이스 ID

### 3. 워크플로우 테스트
1. 설정한 키워드나 단축키 사용
2. 작업 내용 입력
3. Notion 데이터베이스에서 새 항목 확인

## 📝 사용 예시

### 키워드 트리거 사용법:
```
#general 채널에서
!작업 프로젝트 문서 작성하기
```

### 단축키 사용법:
1. Slack에서 `/` 입력
2. 설정한 단축키 선택
3. 작업 내용 입력

## 🔍 문제 해결

### 일반적인 문제들:

1. **웹훅이 작동하지 않는 경우**
   - URL이 올바른지 확인
   - 서버가 실행 중인지 확인
   - 환경변수가 설정되었는지 확인

2. **Notion에 데이터가 추가되지 않는 경우**
   - Notion API Key가 유효한지 확인
   - Database ID가 올바른지 확인
   - Integration이 데이터베이스에 연결되었는지 확인

3. **워크플로우가 실행되지 않는 경우**
   - 트리거 조건이 올바른지 확인
   - 권한 설정 확인
   - 워크플로우가 활성화되었는지 확인

## 📊 모니터링

서버 로그를 확인하여 워크플로우 실행 상태를 모니터링할 수 있습니다:

```bash
# Vercel 로그 확인
vercel logs

# 로컬 개발 시
npm run dev
```

이제 Slack 워크플로우가 시작될 때마다 Notion에 자동으로 작업이 추가됩니다! 🎉
